import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Account",
  description: "...",
};

export default async function Page() {
  return <main>Create account</main>;
}
